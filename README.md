기존 프롬프트는 단순히 '쉽게 써줘'였지만 지금은 독일 Leichte Sprache 규칙집(Netzwerk Leichte Sprache, 2006~)을 한국어에 맞게 옮겨 넣음
  ※ 아이디어를 얻은 Nachrichtenleicht는 한 단계 덜 단순화된 Einfache Sprache를 씀 (종속절 허용).
     그쪽은 명확한 규칙집이 없고 대략적 지침만 있어서, 규칙이 문서로 정리된 Leichte Sprache를 기준으로 삼음
  ※ 복합어 하이픈 분리, 속격/접속법 회피 같은 독일어 전용 규칙은 제외
  한 문장에 한 가지 내용, 문장마다 줄바꿈
  능동태 (~되어진다 같은 피동 회피)
  어려운 말은 바로 다음 줄에서 뜻풀이 (→ 초등학생 대상으로 바꾸면서, 본문에서 풀지 않고 누르면 뜨는 사전으로 대체)
  같은 대상은 늘 같은 단어로 (동의어로 바꾸지 않기)
  비유·관용구·사자성어·반어 금지
  약어 풀어쓰기, 큰 수·백분율은 "열 명 가운데 일곱 명"처럼
  긍정문 우선, 이중 부정 금지
3줄 요약 삭제
대상을 초등학생으로 좁힘
  ※ 실제 개인정보처리방침을 보니 생각보다 친절하게 써 있었음. 다만 초등학생 기준에서는 여전히 어려움
  ※ 개인정보 보호법 제22조의2: 만 14세 미만 아동에게는 알기 쉬운 언어로 알려야 함
  초등학교 교과서 수준의 낱말로 바꾸기
  약관에 자주 나오는 중요한 낱말(개인정보, 동의, 제3자 등)은 바꾸지 않고 남기고, 누르면 뜻이 뜨게 함
  조건과 예외(다만, ~한 경우에는)는 절대 빠뜨리지 않기
본문의 어려운 단어를 누르면 그 자리에서 뜻이 뜨는 기능 추가
대상을 다시 바꿈: 초등학생·약관 → 수행평가로 논문을 읽는 고등학생
  ※ 약관에 써 보니 오히려 가독성이 나빠짐. 표·목록이 줄글로 무너지고, 한 문장 한 줄 규칙이 목록을 길게 늘림
  ※ 결론: 이 도구는 빽빽한 줄글에서 힘을 쓰고, 이미 구조가 잡힌 글에서는 해가 됨
  ※ 논문은 줄글이고, 어려워서 초록만 읽고 넘어가는 내 문제에서 출발. 초록은 이미 요약이므로 요약이 아니라 본문을 쉽게 다시 쓰기
  학술 용어는 바꾸지 않고 남기고 누르면 뜻이 뜨게 함 (인용하고 다시 검색해야 하므로)
  단서와 한계("관련이 있다", "일부 참가자에서")는 지우거나 세게 바꾸지 않기 → 단순화가 곧 왜곡이 되는 것을 막음
  연구의 숫자는 그대로 두기 (Leichte Sprache의 "큰 수는 많은 사람처럼" 규칙은 논문에 해로워서 뺌)
  목적/방법/결과/결론 구간 표시, 영어 논문도 한국어로
붙여넣어 변환하는 칸 추가 (팝업 → 새 탭 reader.html)
  ※ 한국 논문 본문은 대부분 PDF인데, 크롬 PDF 뷰어 안에서는 확장 프로그램이 돌지 않음
실제 논문 단락(다크패턴 선행연구)으로 변환해 원문과 한 줄씩 대조함
  잘 된 점: 숫자·인용 번호 모두 유지, 없는 결과를 지어내지 않음, 선행연구 부분에 [목적] 표시를 억지로 붙이지 않음
  문제: "특히"→"다만"(연결 관계 바뀜), "증가하면서"→"그래서"(없던 인과), "모호하여"→"모호한 편"(주장 약해짐), "등" 누락
  문제: 원문에 없는 약칭(FTC)과 외래어 원어(Platform) 덧붙임, "Interface Interference"가 망가짐
  문제: 문장을 나누다 꾸밈 관계가 끊김, 핵심어 "다크패턴"이 사전에서 빠짐, 사전 뜻풀이가 본문과 어긋남
  원인: 규칙 예시로 "다만"을 적어 둠 / 원어 병기 규칙이 한국어 원문에도 적용됨 / 한 문장 한 내용 규칙이 꾸밈까지 자름
프롬프트 수정: 규칙을 문장 쓰기 / 원문의 뜻 지키기 / 용어와 이름으로 나눔
  연결어는 같은 관계로만 옮기고 새 연결어를 덧붙이지 않기, 주장을 세게도 약하게도 바꾸지 않기
  원어 병기는 다른 언어 원문일 때만, 인명·기관명은 원문 표기 그대로, 핵심 주제어는 사전에 반드시 넣기
  "쉽게 쓰는 것보다 원문의 뜻을 정확히 지키는 것이 더 중요" 명시
2차 시험 (원문을 제대로 넣고 다시 돌림)
  ※ 처음엔 실수로 변환된 글을 다시 넣고 있었음. 입력 칸을 확인해 원문으로 다시 시험
  결과: 뜻은 전부 지킴(특히·따라서·주로·등 유지, FTC 없음, Brignull 그대로) → 그런데 원문을 거의 그대로 옮김. 쉽게 바꾸지 않음
  원인: "쉽게보다 뜻이 더 중요" 우선순위 + 금지 규칙 10여 개 → 모두 확실히 지키는 가장 쉬운 방법이 아무것도 안 바꾸는 것
  깨달음: 쉽게 쓰기와 뜻 지키기는 반대로 당기는 줄다리기. 규칙을 늘린다고 좋아지지 않음
프롬프트 균형 조정
  우선순위 문장 빼고 두 목표를 함께 요구, 원문 문장을 그대로 두면 실패라고 명시
  "뜻을 지킨다 = 낱말을 그대로 둔다"가 아님을 적음. 낱말과 문장 구조는 바꾸고 내용·관계·세기·숫자만 지킴
  연결어 금지를 "원문과 같은 관계일 때만 쓰기"로 완화
  좋은 예·나쁜 예 추가 (답을 알려주지 않게 다크패턴과 상관없는 수면 연구 문장으로)
3차 결과: ~습니다 체·문장 나누기는 됨, 뜻도 지킴. 그런데 낱말은 거의 그대로(예시로 준 "도출", "식별"만 바뀜). 원문에 없던 인용 번호도 붙임
프롬프트를 Leichte Sprache(쉬운 언어) → Einfache Sprache(간단한 언어)로 바꾸고 규칙을 최소한으로 줄임
  ※ Nachrichtenleicht가 실제로 쓰는 단계. 복잡한 정치 기사에 쉬운 언어는 적용이 너무 어려워서 간단한 언어를 골랐다고 함 → 논문도 같은 이유
  ※ 간단한 언어 지침(bpb): 종속절 최대 하나, 외래어 피하기, 어려운 개념 설명은 본문 대신 따로 사전으로, 쉽게 풀면 오히려 길어질 수 있음
  ※ 우리가 만든 "누르면 뜻 뜨는 사전"이 Nachrichtenleicht의 사전 방식과 같음
  규칙 60줄 이상 → 7줄. 숫자("11,000개 가운데 183개")는 연구 결과 자체라 그대로 두기
  시험 단락(다크패턴) 내용을 예시로 쓰지 않음
  결과 창에 규칙 이름 표시 (예: gemini-3.6-flash · 간단한 언어) → 어떤 프롬프트가 돌았는지 헷갈리지 않게
  학술 용어를 남기는 이유 정정: 인용이 아니라 이해가 목적. 쉬운 말로 바꾸면 다른 글에서 같은 용어를 또 모르게 됨
결과 창에 계속 gemini-flash-lite-latest가 뜨는 문제 → 모델 선택 구조 버그 4개
  ① 성공한 모델을 저장해 다음부터 먼저 쓰는 방식이라, 기본 모델이 한 번 실패한 날 lite가 저장되면 이후 3.6-flash가 멀쩡해도 다시 시도하지 않음 (안내도 없음)
  ② 목록 순위에서 latest 별칭을 가장 새 버전으로 보고 버전을 먼저 비교해, lite-latest가 gemini-2.5-pro보다 앞에 섬
  ③ 문구에 "not supported"만 있어도 모델 없음으로 판정 → 지역 제한 같은 다른 오류도 모델 문제로 오분류
  ④ 혼잡이 아닌 이유로 모델이 바뀌면 안내가 뜨지 않음
  수정: 매번 기본 모델부터 시도, "안 되는 모델"만 12시간 기억, 순위는 일반 > lite > preview > gemma, 기본 모델이 아니면 이유와 함께 안내, 예전 저장값 삭제
  ※ 간단한 언어 프롬프트 첫 결과(사전 3개, 낱말 거의 그대로)는 lite 모델로 돈 것이라 프롬프트 평가에서 제외하고 다시 시험
3.6-flash로 돌리니 두 번 연속 "시간 안에 변환을 마치지 못했습니다"
  ※ 지금까지 성공한 시험은 lite로 돌았을 가능성이 큼. 3.6-flash는 긴 단락을 다시 쓰는 데 30초를 넘김
  원인: 완성본을 한 번에 기다리며 30초에 끊고, 같은 모델로 또 부르다가 전체 70초를 다 씀. 30초 제한이 남은 시간도 고려하지 않았음
  수정: 응답을 조각조각 받기(스트리밍). 글이 들어오는 동안은 기다리고 멈췄을 때만 끊음 (첫 조각 60초, 조각 사이 20초)
  시간 초과는 같은 모델로 재시도하지 않고 다음 모델로, 모든 대기는 전체 마감(150초) 안에서
  로딩 15초가 지나면 "긴 글은 1~2분 걸릴 수 있다" 안내
스트리밍 후 시험: gemini-3.5-flash로 성공. 안내 문구로 원인이 보임
  3.6-flash 429(오늘 무료 사용량 소진), 2.5-flash 건너뜀(폐기로 기록됨), flash-latest·3.8·3.7 503(혼잡), pro-latest 429
  ※ 무료 등급 사용량은 모델마다 따로라서, 한 모델이 막혀도 다른 모델로 넘어가면 됨
  문제: 429에도 같은 모델로 재시도(사용량만 버림), 다음 요청에서 또 3.6부터 두드림, 목록 탐색 5번 중 5번째에 겨우 성공
  수정: 429는 재시도하지 않고, 한도에 걸린 모델을 기억해 건너뜀(하루 한도 1시간, 분 한도는 서버가 알려준 대기 시간)
  목록 탐색 기회 5 → 8, 한도로 쉬는 모델은 기회에서 뺌
  안내 문구의 이유를 짧게(사용량 한도 초과(429), 서버 혼잡(503) 등), 자세한 오류는 콘솔에
  스트림이 끝맺음 표시 없이 닫히면 "도중에 끊겨 뒷부분이 빠졌을 수 있다"고 안내
기본 모델을 gemini-3.6-flash → gemini-3.5-flash로 바꿈 (3.6은 무료 사용량 소진, 프롬프트 비교를 같은 모델로 하기 위해)
3.5-flash 기본으로 시험: 한 번은 시간 초과, 한 번은 "AI가 빈 응답을 반환했습니다"
  문제: 빈 응답이면 다른 모델로 넘어가지 않고 포기함 / 시간 초과 알림에는 모델별 이유가 없음 / 어디서 막혔는지 알 방법이 없음
  수정: 빈 응답(차단 제외)이면 다음 모델로 넘어감
  빈 응답 이유에 끝맺음 사유·글 조각 수·생각 조각 수를 적음 → 모델이 생각만 하고 답을 안 쓴 건지 구분
  시간 초과 이유를 "첫 응답 없음(N초)" / "응답 도중 멈춤(N초, N자 받음)"으로 구분
  모든 실패 알림에 모델별 이유 목록을 넣음. 콘솔에는 모델마다 응답 진단 정보를 남김
진단 정보가 붙은 뒤 시험: 결국 gemini-flash-lite-latest로 변환 (탐색 8번째)
  3.5·3.7-flash 503, 3.6-flash·flash-latest 429, 2.5-flash·2.5-pro 404 → 오늘은 좋은 모델이 전부 막힘 (무료 등급 한계)
  버그: 탐색이 음성 받아쓰기(gemini-3.5-transcribe), 음악 생성(lyria-3.5), 음성 대화(gemini-omni-1.1-flash) 모델을 시도함
  원인: 특정 단어가 있는 이름만 빼는 방식이라 새 전용 모델을 못 거름
  수정: gemini-로 시작하고 flash나 pro가 들어간 모델만 고르는 방식으로 뒤집음
  ※ lite 결과는 프롬프트 평가에서 제외 (낱말 거의 그대로, 사전 4개, 오타)
기본 모델을 다시 gemini-3.6-flash로 되돌림 (3.5-flash는 두 번째 후보)
